name: scp copy folder to remote via SSH

on:
  push

jobs:
  build:
    runs-on: ubuntu-latest
    env:
      CI: true
    steps:
      - name: checkout
        uses: actions/checkout@v2
      - name: Run a multi-line script
        run: |
          mkdir ../website
          cp -TR . ../website
          pwd
          ls
          ls ..
      #- name: Setup Tailscale
      #  id: tailscale
      #  uses: tailscale/tailscale-deploy-github@main
      #  with:
      #    authkey: ${{ secrets.TAILSCALE_AUTHKEY }}
      #    version: 1.7.359

      #- name: copy file via ssh
      #  uses:  appleboy/scp-action@master
      #  with:
      #    host: ${{ secrets.HOST }}
      #    port: 22
      #    username: ${{ secrets.SSH_USER }}
      #    #password: ${{ secrets.PASSWORD }}
      #    key: ${{ secrets.KEY }}
      #    source: website/*
      #    target: "~./"

      - name: Deploy to sandbox via rsync
        uses: trendyminds/github-actions-rsync@master
        with:
          INPUT_RSYNC_OPTIONS: -avzr --delete --exclude node_modules --exclude '.git*'
          INPUT_RSYNC_TARGET: /home/drose/
          INPUT_RSYNC_SOURCE: /website/
        env:
          SSH_PRIVATE_KEY: ${{secrets.KEY}}
          SSH_USERNAME: ${{secrets.SSH_USER}}
          SSH_HOSTNAME: ${{secrets.HOST}}